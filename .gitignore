# Local dev artifacts
temporary screenshots/
junk&screenshots/
pw/
*.log

# Tooling / IDE
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db

# AI assistant instructions (kept locally, not part of the deployed site)
CLAUDE.md

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel/
.vercel

# Env
.env
.env.local
.env.*.local

# Source image originals — only the optimized .webp/.jpg are deployed
*_original.*

# Local Node tooling / one-off scripts (Puppeteer harnesses, dev server,
# image converters) — not needed for the static deploy
*.mjs
*.cjs
